# 発表用サマリ 01 — 現時点で作ったもの

*最終更新: 2026-06-10*

---

## プロダクトの一言説明

> **「なんて言えばいいかわからない要望」を、AIが自動でシフト計算のルールに翻訳するエージェント**

---

## 解決したい課題

飲食店のシフト管理では、スタッフからこういう声が日常的に出る：

- 「毎週水曜は習い事があって入れません」
- 「試験期間中はなるべく入れないでほしい」
- 「22時以降のシフトは月3回までにして」

これらを既存のシフト管理ツールで表現しようとすると、**1日ずつ手動で設定する**しかない。
手間がかかる上に、新しいパターンが来るたびに管理者が手作業で対応しなければならない。

---

## このプロダクトの仕組み（3ステップ）

```
① スタッフが自然言語で入力
   「毎週水曜は入れません」

② AIが翻訳 + 未知ルールの場合は自動でコードを書く
   → 既知のルール: すぐ計算に使える形に変換
   → 未知のルール: AIがその場で処理ロジックを自動生成 → 人間が承認

③ OR-Toolsが最適なシフトを計算
   → 全員の希望を満たしつつ、必要人数を確保したシフトを出力
```

---

## ハッカソンのコアポイント（審査員向け）

**「AIが自分でコードを書いて、自分でテストして、人間に承認を求める」**

通常のAIツールは「翻訳するだけ」。
このエージェントは**新しいルールに直面したとき、自分でその処理ロジック（ハンドラ）を書き、テストし、動いたら人間に承認を求める**。
承認後は次回から自動で使い回す。これが自律エージェントの証明。

---

## 現在の完成状況（2026-06-10）

| コンポーネント | 状態 | 内容 |
|---|---|---|
| データモデル定義 | ✅ 完成 | 16種の制約タイプをPydanticで型定義 |
| 自然言語パーサ | ✅ 動作中 | Gemini Flash（思考オフ）で既知/未知を振り分け。確信度<0.5は「分かったフリをしない」 |
| 備考(note)のAI解釈 | ✅ 動作中 | CSVの日ごと備考をバッチ解釈→ ✅時間補正 / 🆕新ルール候補（承認キューへ）/ ⚠️申し送り の3分類 |
| AIハンドラ自動生成（L2） | ✅ 動作中 | 未知タイプに Gemini Pro がコード生成 → サンドボックスでテスト → 人間が承認 → ソルバーに登録 |
| シフト計算エンジン | ✅ 動作中 | OR-Tools。解けない時も不足最小の暫定シフトを必ず出力（充足スコア100点満点） |
| シフト検証器（独立検算） | ✅ 動作中 | 「入れない人を入れる」等の不変条件違反をゼロ件と証明して表示 |
| デモUI（5画面） | ✅ 動作中 | 設定→出勤希望CSV→要望→計算・確認→管理者承認（デモの流れと同じ並び） |
| 承認→自動再計算（ループ完成） | ✅ 動作中 | 承認→原文をparams化→再計算で反映。before/after差分で「水曜が消えた」を表示（T2） |
| ハンドラ網羅（残り13type） | 🔜 中盤 | 「複雑な要件を完全充足」の根拠づくり（T3） |
| クラウドデプロイ＋Supabase | 🔜 最終 | Google Cloud Run ×2（T5/T6） |

> **2026-06-10 達成①（T1）**: **CSVの備考欄からも未知ルールを検出**。
> 「毎週水曜は習い事で入れません」「毎週水曜は大学の授業があります」という**別々の言い回し**を
> AIが同じ `recurring_day_off` として集約し、承認キューに1件で登録（バッチ1回・実測¥0.35/2回）。
>
> **2026-06-10 達成②（T2）= L2ループ一周が完成**:
> 承認すると、AIが各人の原文を params（`{person_id, weekday}`）に変換し、
> 登録したハンドラの「材料」として供給 → 再計算で **p01 の水曜の割当が実際に消える**。
> 承認直後に before/after 差分で変化を可視化。「未知検出→生成→テスト→承認→反映」が端から端まで繋がった。

---

## 画面構成（マルチページ・ハブ `src/ui/app.py` で1つのURLに統合）

`python -m streamlit run src/ui/app.py` で起動し、サイドバーから切り替える。

```
（画面の並びはデモの流れと同じ：設定→出勤希望→要望→計算→承認）

① セットアップ          ✅ マスタCSVアップロード + 営業情報フォーム（初期値=11月サンプル）
② 出勤希望CSV           ✅ desired_shifts.csv→availability保存＋備考のAI解釈（✅時間補正/🆕新ルール候補→承認キュー/⚠️申し送り）
③ シフト作成の要望       ✅ 全体方針(新人はキッチン優先等)+横断ルール+特殊事情 → 反映済み/確認中
④ シフト計算・確認       ✅ 計算→シフト表＋充足スコア(100点満点)＋不変条件チェック(独立検算)＋評価指標(充足/公平性/希望消化率)＋暫定/確定
⑤ 管理者承認            ✅ 未知→AIがハンドラ生成→テスト→承認/却下（デモのクライマックス）

サイドバーに Gemini セッション消費（トークン・概算料金）を常時表示。
```

> **役割整理（2026-06-04）**: ②は「CSVの列に収まらない例外・人間関係・繰り返し・未知ルール」
> （経路C）の入力に専念。日ごとの希望（経路B）はCSV、シフト表の表示は⑤、と画面の責務を分離した。

---

## 技術スタック

| 役割 | 技術 |
|---|---|
| バックエンドAPI | FastAPI (Python) |
| データ型定義 | Pydantic v2 |
| シフト計算エンジン | OR-Tools (Google製 最適化ソルバー) |
| AI（自然言語処理・コード生成） | Gemini API (Flash + Pro) |
| デモUI | Streamlit |
| データベース | Supabase (PostgreSQL) |
| インフラ | Google Cloud Run |
